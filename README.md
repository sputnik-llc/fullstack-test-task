## Выполненное тестовое задание на позицию Fullstack разработчика (Python + React)

### Выполненные изменения

Архитектура и рефакторинг

- Файлы бэкенда разбиты на модули: API, core, db, models, schemas, service, tasks
- Фронтенд разбит на компоненты и логические части  
- Внедрен DI для работы с БД  
- Добавлен Pydantic Settings для конфигурации
- Устранено дублирование кода, вынесена общая логика  

Оптимизация и багфикс

- Добавлена пагинация через `count(*) OVER()` (1 запрос вместо 2) 
- Добавлены индексы для работы с БД
- Исправлены баги при работе с Celery  
- Рекурсивные вызовы заменены на `celery.chain`
- Исправлен баг: `scan_file_for_threats = None`
- Добавлен `.env.production` (фикс запуска фронта)  
- Обновлены Dockerfile и docker-compose 

### Рекомендуемые улучшения

- Покрыть проект unit- и интеграционными тестами  
- Загрузка файлов по чанкам
- Async I/O (`aiofiles`)
- Проверка MIME-типа по сигнатуре файла
- Обновление статуса обработки в реальном времени

### Запуск
1. ```docker compose -f docker-compose.dev.yml up```
2. ```docker exec -it backend alembic upgrade head```

### Frontend
```http://localhost:3000/test``` 
### Backend
```http://localhost:8000/docs```
